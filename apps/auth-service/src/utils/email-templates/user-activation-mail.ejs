<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #f0f2f5; /* Light gray background like Google */
            font-family: 'Google Sans', Roboto, Arial, sans-serif;
        }

        .wrapper {
            width: 100%;
            table-layout: fixed;
            background-color: #f0f2f5;
            padding-bottom: 40px;
        }

        .main-card {
            max-width: 520px; /* Google uses slightly narrower cards */
            margin: 40px auto;
            background-color: #ffffff;
            border: 1px solid #dadce0;
            border-radius: 8px;
            padding: 40px;
            text-align: center;
        }

        .logo {
            font-size: 24px;
            font-weight: 500;
            color: #5f6368;
            margin-bottom: 24px;
        }

        /* The signature blue "Nex" and black "Buy" */
        .brand-n { color: #4285F4; }
        .brand-e { color: #EA4335; }
        .brand-x { color: #FBBC05; }

        h1 {
            color: #202124;
            font-size: 22px;
            font-weight: 400;
            margin-bottom: 24px;
            line-height: 1.4;
        }

        .message {
            color: #3c4043;
            font-size: 14px;
            line-height: 1.5;
            text-align: left;
            margin-bottom: 30px;
        }

        .otp-box {
            font-size: 32px;
            color: #1a73e8; /* Google Blue */
            font-weight: 500;
            letter-spacing: 4px;
            padding: 16px;
            background-color: #f8f9fa;
            border-radius: 4px;
            display: inline-block;
            margin: 10px 0;
        }

        .footer {
            max-width: 520px;
            margin: 0 auto;
            text-align: center;
            color: #70757a;
            font-size: 12px;
        }

        .footer a {
            color: #1a73e8;
            text-decoration: none;
        }

        .divider {
            height: 1px;
            background-color: #dadce0;
            margin: 30px 0;
        }
    </style>
</head>
<body>
    <div class="wrapper">
        <div class="main-card">
            <div class="logo">
                <span style="font-weight: bold; color: #202124;">NexBuy</span>
            </div>

            <h1>Verify your email address</h1>

            <div class="message">
                Hello <%= name %>,<br><br>
                To help keep your NexBuy account secure, we need to verify your email address.
                Use this code to complete your setup:
            </div>

            <div class="otp-box">
                <%= otp %>
            </div>

            <div class="message" style="margin-top: 30px; font-size: 13px; color: #70757a;">
                This code will expire in 5 minutes. If you didn't request this, you can safely
                ignore this email.
            </div>

            <div class="divider"></div>

            <div class="message" style="font-size: 12px; color: #70757a;">
                You received this email because it is a mandatory update regarding your account security.
            </div>
        </div>

        <div class="footer">
            <p>Sent by NexBuy Inc., 1600 Amphitheatre Parkway, Mountain View, CA</p>
            <p><a href="#">Privacy Policy</a> • <a href="#">Terms of Service</a></p>
        </div>
    </div>
</body>
</html>